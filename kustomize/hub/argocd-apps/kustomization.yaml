# Reference manifests — prefer helm/gitops-bootstrap with git.repoURL set.
# These are applied manually when not using GitOps.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - hub-postgres.yaml
  - models.yaml
