apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: openshift-gitops-operator-group
  namespace: {{ .Values.openshiftGitOps.operatorNamespace }}
spec:
  # AllNamespaces — GitOps does not support OwnNamespace (targetNamespaces: [this-ns]).
  upgradeStrategy: Default
